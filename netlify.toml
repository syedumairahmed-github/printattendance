[build]
  command = "vite build"
  publish = "dist/client"

[build.environment]
  NODE_VERSION = "22"
  NPM_FLAGS = "--legacy-peer-deps"
[dev]
  command = "npm run dev"
  targetPort = 3000
  port = 8888
